<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.edeasy">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/main_yellow_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/main_yellow_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Dashboard_Activity"
            android:label="Dashboard"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".NavDrawer"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.VIEW"/> -->
            <!-- <category android:name="android.intent.category.BROWSABLE"/> -->
            <!-- <data android:host="www.google.com" android:scheme="http"/> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".CurrentRoutine"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PreviousResults"
            android:label="Previous Results"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Settings"
            android:label="@string/title_activity_settings"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Chatroom"
            android:label="@string/title_activity_chatroom"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CurrentMarksheet"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CourseMaterials" />
        <activity
            android:name=".CalendarDisplay"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NotificationsDisplay"
            android:label="Notifications"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Welcome"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignUp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ChatroomCourse1" />
        <activity android:name=".ChatroomCourse2" />
        <activity android:name=".ChatroomCourse3" />
        <activity android:name=".ChatroomCourse4" />
        <activity android:name=".ChatroomCourse5" />
        <activity android:name=".FullRoutine"
            android:screenOrientation="landscape"/>
    </application>

</manifest>